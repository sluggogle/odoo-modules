<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="website_product_not_available_product_template_form" model="ir.ui.view">
        <field name="name">website.product.not.available.product.template.form</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="website_sale.product_template_form_view" />
        <field name="arch" type="xml">
            <field name="website_id" position="after">
                <field name="website_availability"/>
                <field name="website_not_available_message" attrs="{'invisible': [('website_availability', '=', True)]}"/>
            </field>
        </field>
    </record>

</odoo>
